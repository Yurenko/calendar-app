import { Box } from '@mui/material'

import styles from '~/components/calendar/Calendar.module.css'

const Calendar = () => {
  return <Box className={styles.wrapper}>Calendar</Box>
}

export default Calendar
